# MaskForge

MaskForge is a local document scrubbing tool designed for safe AI collaboration.

It replaces sensitive words with unique mask tokens and allows full restoration using a mapping file.

---

## ✨ Features

- GUI interface (Tkinter)
- Drag & drop support
- Batch processing
- Supports:
  - .docx
  - .pptx
  - .xlsx
  - .txt
- Automatic JSON mapping generation
- AI prompt copy helper
- 100% local processing (no cloud upload)

---

## 🚀 Installation

```bash
pip install -r requirements.txt